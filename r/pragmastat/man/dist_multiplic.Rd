\name{dist_multiplic}
\alias{dist_multiplic}
\title{Multiplicative (Log-Normal) Distribution}
\description{
Create a multiplicative (log-normal) distribution.
Samples are generated by exponentiating normal samples.
}
\usage{
dist_multiplic(log_mean, log_std_dev)
}
\arguments{
\item{log_mean}{Mean of the underlying normal distribution}
\item{log_std_dev}{Standard deviation of the underlying normal distribution (must be positive)}
}
\value{
A Distribution object with the following methods:
\itemize{
  \item \code{sample(rng)}: Generate a single sample
  \item \code{samples(rng, count)}: Generate multiple samples
}
}
\examples{
r <- rng(1729)
dist <- dist_multiplic(0, 1)
dist$sample(r)
}
