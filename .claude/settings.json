{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'mise (run )?publish'; then echo 'BLOCKED: publish is a protected operation requiring manual execution' >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
